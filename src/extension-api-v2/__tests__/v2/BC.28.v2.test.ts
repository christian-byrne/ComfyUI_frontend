// DB cross-ref: BC.28 — S9.SG1
// blast_radius: 16.89  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.28 v2 — Subgraph fan-out via set/get virtual nodes', () => {
  it.todo('NodeHandle for a subgraph input virtual node exposes comfyClass reflecting the virtual node type')
  it.todo('defineNodeExtension nodeCreated receives NodeHandle for virtual set/get nodes within subgraph scope')
  it.todo('NodeHandle.entityId is stable across subgraph boundary crossings for the same virtual node instance')
})
