// DB cross-ref: BC.05 — S4.W2,S2.N11
// blast_radius: 33.35  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.05 v2 — Custom DOM widgets and node sizing', () => {
  it.todo('onNodeMounted callback is invoked with the NodeHandle after the node DOM is ready')
  it.todo('NodeHandle.getSize() reflects updated height after a custom DOM widget is inserted')
  it.todo('WidgetHandle obtained inside onNodeMounted has a valid widgetType and name')
})
