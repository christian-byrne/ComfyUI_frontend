// DB cross-ref: BC.34 — S12.UI3
// blast_radius: 0.0  compat-floor: no
import { describe, it } from 'vitest'

describe('BC.34 v2 — Settings-panel custom dialog integration', () => {
  it.todo('defineExtension shell CommandManager can register a command that opens a settings dialog without direct DOM manipulation')
  it.todo('settings dialog integration via v2 shell API renders a Vue component provided by the extension, not injected HTML')
  it.todo('settings registered via v2 shell are discoverable through CommandManager without coupling to settings-panel DOM structure')
})
