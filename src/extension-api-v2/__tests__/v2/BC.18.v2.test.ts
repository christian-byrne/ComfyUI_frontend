// DB cross-ref: BC.18 — S6.A3
// blast_radius: 22.74  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.18 v2 — Backend HTTP calls', () => {
  it.todo('defineExtension setup exposes an api object with a fetchApi(path, init?) method')
  it.todo('fetchApi prefixes the path with the ComfyUI server base URL automatically')
  it.todo('fetchApi returns a Promise<Response> compatible with the Fetch API standard')
})
