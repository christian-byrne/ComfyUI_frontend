// DB cross-ref: BC.08 — S10.D2
// blast_radius: 11.81  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.08 v2 — Programmatic linking', () => {
  it.todo('NodeHandle exposes an API to programmatically create a link between two nodes by slot index')
  it.todo('programmatic link creation fires "connected" on the target NodeHandle')
  it.todo('linking to an incompatible slot type is rejected without throwing an unhandled error')
})
