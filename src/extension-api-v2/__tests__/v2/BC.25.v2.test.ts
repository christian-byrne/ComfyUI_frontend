// DB cross-ref: BC.25 — S12.UI1
// blast_radius: 10.98  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.25 v2 — Shell UI registration (commands, sidebars, toasts)', () => {
  it.todo('defineExtension shell.commandManager.registerCommand registers a command retrievable by id')
  it.todo('defineExtension shell with SidebarTabExtension adds a sidebar tab accessible by extensionId')
  it.todo('defineExtension shell with BottomPanelExtension adds a bottom panel accessible by extensionId')
  it.todo('defineExtension shell.toastManager.add displays a toast with the provided message and severity')
})
