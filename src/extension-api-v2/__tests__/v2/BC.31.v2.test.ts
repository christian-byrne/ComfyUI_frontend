// DB cross-ref: BC.31 — S16.DOM1,S16.DOM2,S16.DOM3,S16.DOM4
// blast_radius: 0.0  compat-floor: no
import { describe, it } from 'vitest'

describe('BC.31 v2 — DOM injection and style management', () => {
  it.todo('v2 path for DOM injection is deferred — direct DOM manipulation has no v2 replacement yet; extensions should use SidebarTabExtension or BottomPanelExtension shell APIs instead')
  it.todo('v2 path for style injection is deferred — scoped CSS via Vue SFC is the intended replacement but no bridge API exists yet')
  it.todo('defineExtension shell SidebarTabExtension renders a Vue component without raw DOM injection')
})
