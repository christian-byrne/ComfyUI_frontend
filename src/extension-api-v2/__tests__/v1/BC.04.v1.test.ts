// DB cross-ref: BC.04 — S2.N10,S2.N17,S2.N19
// blast_radius: 38.07  compat-floor: yes
import { describe, it } from 'vitest'

describe('BC.04 v1 — Node interaction: pointer, selection, resize', () => {
  it.todo('nodeType.prototype.onMouseDown receives canvas pointer events with correct local coordinates')
  it.todo('nodeType.prototype.onSelected and onDeselected are called when the node enters and leaves selection')
  it.todo('nodeType.prototype.onResize is called with updated size after a user drag-resize')
  it.todo('nodeType.prototype.onDblClick fires on canvas double-click and can open a custom modal')
})
